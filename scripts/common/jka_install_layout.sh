# shellcheck shell=bash
#
# scripts/common/jka_install_layout.sh
#
# PR-A placeholder. The real install-layout helpers will be extracted
# from scripts/install_taystjk.sh in PR-B, alongside the runtime shim
# split. This file exists in PR-A only to reserve the location and to
# give `bash -n` a non-empty target.
#
# Requires: jka_runtime_common.sh sourced first (for `fail`, `info`).

_jka_install_layout_pr_a_placeholder() {
  :
}
